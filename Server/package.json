{
  "name": "server",
  "version": "1.0.0",
  "description": "Paquetes node js, npm.\r Paquetes de React-router-dom (solo si es necesario)\r Paquetes de nodemon (npm i nodemon -g) installacion global\r Paquetes de dotenv (npm i dotenv)\r Paquetes de express, mongoose, cors (npm i express mongoose cors)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^4.1.4",
    "mongoose": "^6.0.13"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
